import 'package:flutter/material.dart';

class AccountDetailSheet extends StatelessWidget {
  const AccountDetailSheet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
